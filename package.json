{
  "name": "codex-token-vault-windows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --no-warnings --test test/*.test.mjs",
    "benchmark": "node --no-warnings tools/benchmark.mjs"
  }
}
